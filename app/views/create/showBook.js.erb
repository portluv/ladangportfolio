if(showBookElements==null){
    modal.style.opacity = "0";
    modal.style.display = "block";
    modal.style.opacity = "1";
    
    showBookElements='<%= escape_javascript(render :partial => 'bookView', :locals => {:thing => @thing}) %>'
    $('#modal').prepend(showBookElements);
}